= simple_form_for(@<%= singular_table_name %>, :html => {:class => 'form'}) do |form|
  <%- attributes.each do |attribute| -%>
    = form.<%= attribute.reference? ? :association : :input %> :<%= attribute.name %>
  <%- end -%>

  = form.button :submit, :class => 'btn btn-primary'
